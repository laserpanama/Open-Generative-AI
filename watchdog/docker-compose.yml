version: "3.9"

services:
  watchdog:
    build: .
    restart: unless-stopped
    env_file: .env
    volumes:
      - watchdog_data:/data
    environment:
      DB_PATH: /data/panamacompra_tenders.db

volumes:
  watchdog_data:
